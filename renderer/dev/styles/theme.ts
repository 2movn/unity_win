// Shared theme styles for the entire application
export const gradientStyles = {
  background: undefined as unknown as string,
  backgroundCard: undefined as unknown as string,
  backgroundButton: undefined as unknown as string,
  backgroundButtonHover: undefined as unknown as string,
  backgroundModal: undefined as unknown as string,
  textColor: undefined as unknown as string,
  textColorSecondary: undefined as unknown as string,
  borderColor: undefined as unknown as string,
  shadowColor: undefined as unknown as string,
  primaryGradient: undefined as unknown as string,
  secondaryGradient: undefined as unknown as string,
  cardGradient: undefined as unknown as string,
  buttonGradient: undefined as unknown as string,
  buttonHoverGradient: undefined as unknown as string,
  siderGradient: undefined as unknown as string,
  headerGradient: undefined as unknown as string
};

export const customStyles = {
  mainContainer: {
    // Trả về mặc định: không ép nền hay màu
    minHeight: '100%',
    padding: '24px'
  },
  card: {
    // Dùng Card mặc định của AntD
  },
  button: {
    // Dùng Button mặc định của AntD
  },
  buttonHover: {
    // Không custom hover
  },
  table: {
    // Dùng Table mặc định
  },
  tag: {
    // Dùng Tag mặc định
  }
};

export const tableStyles = {
  table: {
    // Mặc định
  },
  header: {
    // Mặc định
  },
  body: {
    // Mặc định
  },
  hover: {
    // Mặc định
  }
};

export const modalStyles = {
  content: {
    // Mặc định
  },
  header: {
    // Mặc định
  },
  title: {
    // Mặc định
  },
  body: {
    // Mặc định
  },
  footer: {
    // Mặc định
  }
};

// Global styles for better text readability
export const globalStyles = '';
